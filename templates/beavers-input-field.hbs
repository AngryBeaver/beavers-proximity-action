<div class="form-group">
    <label>{{label}}</label>
    <div class="form-fields">
        {{#if (eq type "area")}}
            <textarea {{#if disabled}}disabled="true"{{/if}} style="resize:vertical" name="{{name}}"
                      {{#if defaultValue}}placeholder="{{defaultValue}}"{{/if}}
            >{{value}}</textarea>
        {{else if (eq type "none")}}
        {{else if (eq type "selection")}}
            <beavers-selection>
                <select name="{{name}}" {{#if disabled}}disabled="true"{{/if}}>
                    {{#select value}}
                        <option selected disabled value=""></option>
                        {{#each choices}}
                            <option value="{{@key}}" img="{{this.img}}">
                                {{this.text}}
                            </option>
                        {{/each}}
                    {{/select}}
                </select>
            </beavers-selection>
        {{else}}
            <input {{#if disabled}}disabled=true{{/if}} {{#if defaultValue}}placeholder="{{defaultValue}}"{{/if}}
                   value="{{value}}" name="{{name}}" type="{{type}}"/>
        {{/if}}
    </div>
    {{#if note}}
        <p class="notes">{{note}}</p>
    {{/if}}
</div>