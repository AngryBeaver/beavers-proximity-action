export class directedShapeSquareGrid {
    distance:number;
    origin:Point;
    rotation:number;
    //TODO get grids for cone shaped on grids
    //highlight via highlight layer needs measuredTemplate -> grid.grid.highlightGridPosition(hl, {x, y, border, color});

}